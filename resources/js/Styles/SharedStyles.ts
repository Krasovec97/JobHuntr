import styled from "styled-components";

export let AddressSelection = styled.div`
    border-radius: 7px;
    cursor: pointer;
    transition: all ease-in-out 100ms;
        &:hover {
            background: #d30855;
            color: white;
        }
    `
